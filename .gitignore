# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environment
.venv/
venv/
env/
ENV/

# Vercel
.vercel/

# Node / Next.js (build artifacts — never commit)
node_modules/
.next/
next-env.d.ts


# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Secrets (don't commit!)
config.env
.env.local

# Telegram session (don't commit!)
dikaai_session.session
dikaai_session.session-journal

# Database (too large for git, use external storage)
*.db
*.db-shm
*.db-wal

# Training data + checkpoints (runtime artifacts, regenerated in Colab)
model_checkpoints/
model_checkpoints/*.json
model_checkpoints/*.pt
training_history.csv
vocab.json
dikaai.db*
*.session*
config.env
.env.local
